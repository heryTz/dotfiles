general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session    # lock before suspend.
    # NOTE: must be the table form. hl.dsp.dpms("on") silently TOGGLES (a non-table
    # arg falls through to toggle), which would blank the screen on wake.
    after_sleep_cmd = hyprctl dispatch 'hl.dsp.dpms({ action = "on" })'  # to avoid having to press a key twice to turn on the display.
}

listener {
    timeout = 300 
    on-timeout = hyprlock
}
