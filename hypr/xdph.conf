screencopy {
    # Pre-tick "allow restore token" in hyprland-share-picker so a screenshare
    # selection is remembered instead of re-prompting on every ScreenCast session.
    allow_token_by_default = true
}
